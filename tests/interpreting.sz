task main {
    // this is the main entrypoint of the program
    forced_assignations()
}

task conditions {
    const a = true

    if (a) {
        std::println("TRUE")
    } else {
        std::println("FALSE")
    }
}

task forced_assignations {
    const d = true
    d* = false
    std::println(d)
}